<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.PermissionMapper">

    <resultMap id="PermissionResultMap" type="com.example.demo.entity.Permission">
        <id property="id" column="id" />
        <result property="permissionName" column="permission_name" />
        <result property="url" column="url" />
    </resultMap>

    <select id="listByUsername" resultMap="PermissionResultMap">
        SELECT p.id,p.permission_name,p.url
        FROM permission p,role_permission rp,role r,users_role ur,users u
        WHERE p.id=rp.permission_id AND rp.role_id=r.id AND r.id=ur.role_id AND ur.users_id=u.id AND u.username=#{username}
    </select>
</mapper>
